name: My Workflow

on: [push]  # 指定触发事件，这里是每次代码推送时触发

jobs:
  example-job:
    runs-on: ubuntu-latest  # 使用最新的Ubuntu运行器

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3  # 使用最新版的 actions/checkout

    - name: Run go.sh
      run: ./go.sh  # 运行脚本
